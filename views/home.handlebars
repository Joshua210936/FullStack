<head>
    <link rel="stylesheet" href="/css/home.css">
</head>

<section id="hero">
    <div class="hero-text">
        <h1>Find Your Dream Home</h1>
        <p>Your perfect home is just a click away. Browse through thousands of listings.</p>
        <a href="/buyHouse"><button id="cta-button">Get Started</button></a>
    </div>
</section>

<section id="about">
    <h2>About Us</h2>
    <p>We are committed to helping you find your dream home with ease and convenience.</p>
    {{#each advertisements}}
    <section id="advertisement-banner">
        <div class="advertisement-banner-content">
            <div class="banner-left">
                <img src="/images/{{this.ad_image}}" alt="{{this.ad_title}}" class="banner-image">
            </div>
            <div class="banner-right">
                {{#if this.agent}}
                <img src="images/{{this.agent.agent_image}}" alt="{{this.agent.agent_firstName}}" class="agent-image">
                <h1>{{this.agent.agent_firstName}} {{this.agent.agent_lastName}}</h1>
                {{#each this.description}}
                <ul>
                    <li>
                        {{this}}
                    </li>
                </ul>

                {{/each}}
                {{else}}
                No Agent Assigned
                {{/if}}

                <a href="/propertyAgentProfile/{{this.agent.agent_id}}"><button class="btn btn-info">View Profile</button></a>
            </div>
        </div>
    </section>
    {{/each}}

</section>

<section id="services">
    <h2>Our Services</h2>
    <div class="service-cards">
        <div class="card">
            <h3>Buying</h3>
            <p>We provide a wide range of listings to help you find the perfect home.</p>
        </div>
        <div class="card">
            <h3>Selling</h3>
            <p>Get the best deals and a seamless selling experience with our expert agents.</p>
        </div>
        <div class="card">
            <h3>Renting</h3>
            <p>Explore a variety of rental properties that suit your needs and budget.</p>
        </div>
    </div>
</section>